@model PortfolioMVC5v3.Models.ExtraInformation

@{
    ViewBag.Title = "Karta dodatkowej informacji";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

@section AdditionalCss
{
}

@Html.HiddenFor(m => m.ExtraInformationId)

<div class="row h-100">
    <div class="col-12">
        <div class="card" style="height: 90%">
            <div class="card-body d-flex flex-column">
                <h2 class="card-title">Karta dodatkowej informacji</h2>

                <div class="row mt-3">
                    <div class="col-12 mb-2">
                        <h5>Opcje główne:</h5>
                    </div>

                    <div class="col-6 pl-4">
                        <div class="form-group">
                            <label for="Title">Nazwa</label>
                            <input type="text" class="k-textbox w-100" name="Title" id="Title" placeholder="Nazwa" value="@Model.Title" />
                            <small id="TitleHint" class="form-text text-muted">Pełna nazwa dodatkowej informacji</small>
                        </div>
                    </div>

                    <div class="col-6">
                        <div class="form-group">
                            <label for="Type">Typ</label>
                            <input id="Type" value="@Model.Type" style="width: 100%;" />
                            <small id="TypeHint" class="form-text text-muted">Kategoria dodatkowej informacji</small>
                        </div>
                    </div>
                </div>

                <div class="row mt-3">
                    <div class="col-12 mb-2">
                        <h5>Opcje dodatkowe:</h5>
                    </div>
                    <div class="col-12 pl-4">
                        <table>
                            <tr>
                                <th class="py-2"><input id="ShowInCv" /></th>
                                <th class="py-2"><label for="ShowInCv" class="ml-3 pt-2">Pokazuj w CV</label></th>
                            </tr>
                        </table>
                    </div>

                </div>

                <div class="row mt-auto">
                    <div class="col">
                        <button type="button" name="SaveBtn" id="SaveBtn" class="btn btn-success float-right">
                            <i class="fas fa-save mr-2"></i> Zapisz zmiany
                        </button>
                    </div>
                </div>

            </div>
        </div>
    </div>
</div>


@section scripts
{
    <script>
        var showInCv = @Html.Raw(Json.Encode(@Model.ShowInCv));
    </script>

    <script src="~/Scripts/admin/ExtraInformation/Card.js"></script>
}
